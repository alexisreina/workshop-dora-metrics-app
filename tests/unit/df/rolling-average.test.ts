import { describe, it, expect } from 'vitest';

describe('Rolling average', () => {
  it('computes 7-day rolling average with partial window', () => {
    expect(true).toBe(true); // Placeholder until implementation
  });
});
